# config-web-scraper

Reusable, config-driven CLI scraper: describe what to extract in YAML, get
CSV or JSON back. No code changes per site.

_Full README with config schema, screenshot, and example output lands here
once the implementation is wired up._
